# `FayneAldan/prettier-config`

> My personal [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ npm install --save-dev FayneAldan/prettier-config
```

**Edit `package.json`**:

```jsonc
{
	// ...
	"prettier": "FayneAldan/prettier-config"
}
```
